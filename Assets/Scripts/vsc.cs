using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class vsc : MonoBehaviour
{
    [SerializeField] private int myint = 0;
    // Start is called before the first frame update
    void Start()
    {
        myint = 9;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
